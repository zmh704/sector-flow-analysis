# GP分析系统｜项目维护摘要

> **用途**：后续修改前优先阅读本文件，先定位模块和数据链路，再按需打开源码。避免每次全量读取项目。
>
> **摘要版本**：2026-08-06
> **当前分支**：`master`
> **最新提交**：`63ada6a tune: disable min inflow days condition in today leaders`
> **远端**：`https://github.com/zmh704/sector-flow-analysis.git`
> **最近验证**：76/76 测试通过；`npm run check` 通过。
> **注意**：工作区存在未提交变更（已删除 `代碼審查與優化建議.md`、`項目分析與優化建議.md`，新增未跟踪的本文档）。

---

## 1. 项目定位

GP分析系统是一个原生 JavaScript + Chart.js 的 A 股行业/概念板块资金流向分析页面：

- 输入：Excel 或 schema v3 JSON。
- 输出：行业/概念资金流向图、今日推荐、关注板块、趋势对比和个股行情。
- 后端：Node.js 原生 HTTP 服务器，无 Express、无前端框架、无构建步骤。
- 数据口径：以 A 股成交额 TOP200 股票资金流向数据为主。
- 主链路：`Excel → analyze.js → schema v3 JSON → lib/manifest.js → data.js → calc.js/leaders.js/charts.js`。

---

## 2. 启动与常用命令

```bash
npm install
npm start                 # 默认 http://127.0.0.1:3001
npm test                  # Node 内置测试，当前约 76 项
npm run check             # 全部 JS 语法检查
npm run generate:list     # 生成 list.json
npm run reprocess         # 历史迁移 dry-run
node reprocess.js --apply # 明确写入历史迁移结果
npm run convert:v3        # schema v3 转换 dry-run
npm run convert:v3:apply  # schema v3 转换并写入
```

Windows 可双击 `start.cmd`。服务器默认只监听 `127.0.0.1`；可用 `HOST`、`PORT` 覆盖。

**提交/推送约定**：GitHub TLS 吊销检查可能失败，推送使用：

```bash
git -c http.sslBackend=openssl push origin master
```

不要设置 `http.sslVerify=false`。

---

## 3. 前端脚本加载顺序与职责

`index.html` 底部脚本顺序不能随意调整：

```text
chart.umd.min.js
→ config.js
→ stock-utils.js
→ calc.js
→ data.js
→ charts.js
→ leaders.js
→ modals.js
→ app.js
```

| 文件 | 职责 | 修改入口 |
|---|---|---|
| `index.html` | 页面 DOM、今日推荐/关注板块开关、弹窗骨架、脚本入口 | 增加控件或调整结构 |
| `style.css` | 页面、图表、标签、弹窗、悬浮便签样式 | 视觉和布局 |
| `config.js` | 全局阈值、条件开关、状态和缓存变量 | 业务阈值首选修改处 |
| `stock-utils.js` | 股票代码、市场、单位、旧/新 schema 解析 | 股票兼容逻辑 |
| `calc.js` | 股票/板块连续天数、成交额/价格条件、关注板块、股票-板块映射 | 筛选计算 |
| `data.js` | manifest、日期排序、数据加载、索引、LRU、日期切换 | 数据生命周期 |
| `charts.js` | 行业/概念主图、前一日虚线、整行点击 | 主图展示与点击 |
| `leaders.js` | 今日推荐和关注板块结构化数据/HTML | 首页推荐与关注板块 |
| `modals.js` | 查看全部、趋势、个股、全部股票、预选页签 | 弹窗和表格 |
| `app.js` | 初始化、事件绑定、Excel 上传、便签 | 页面事件和入口 |

---

## 4. 数据结构与加载链路

### 4.1 schema v3

每日 JSON 顶层核心字段：

```text
schemaVersion
交易日期
生成时间
数据来源
股票字典
行业板块资金流向
概念板块资金流向
分析总结
```

`股票字典` 以稳定 `stockKey` 保存每日股票记录：

```text
stockKey, name, code
amountYi, netYi, changePct, volumeWanShou
high, open, low, close, avg5, avg10
```

行业/概念板块行通过 `股票键` 引用字典，不再重复保存股票明细。页面通过 `StockUtils.getSectorStocks()` 水合股票；同时兼容旧 schema 的 `涉及股票` 文本和 schema v2 的 `股票明细`。

### 4.2 manifest 选择规则

`lib/manifest.js` 扫描 `data/` 中匹配 `板块资金流向*.json` 的文件：

1. 优先 JSON 字段 `交易日期`。
2. 再从文件名识别 ISO/中文日期。
3. 同一交易日只保留一份：`schemaVersion` 高者优先，其次含价格数据者优先，再其次 ISO 文件名优先。
4. 输出 `list.json` / `/api/list` 的 manifest v2。
5. 目录指纹未变化时，`server.js` 复用 manifest 缓存。

### 4.3 浏览器加载策略

`data.js`：

- 首屏加载最近 4 个交易日。
- 后台补齐当前日期的 `DATA_ANALYSIS_DAYS=12` 日窗口。
- 点击历史日期时按需补载窗口。
- `MAX_LOADED_DATES=20`，超出后按 LRU 回收，当前日期和分析窗口受保护。
- `DATE_BUTTON_LIMIT=10`：日期选择器默认只展示最近 10 个交易日（历史日期可通过加载后展开）。
- `storeDataForDate()` 会预解析股票，并构建 `_stockFieldIndex`、`_stockKeysByDate`、名称到 key 的索引（`_stockNameKeyIndex`）。
- 数据覆盖只清理被覆盖日期对应的股票索引，不再全量遍历所有股票。

关键缓存及失效：

| 缓存 | 内容 | 失效位置 |
|---|---|---|
| `_stockFieldIndex` | 股票按日期的 volume/net/amount/价格/均线 | 全量重置或索引重建 |
| `_stockDaysCache` | 股票连续净流入天数 | 切换日期、补载、覆盖数据 |
| `_consecutiveInflowCache` | 板块连续净流入天数 | 切换日期、补载、覆盖数据 |
| `_sectorFilterCache` | 关注板块筛选结果 | 切换日期、数据变化、关注开关变化 |
| `_focusDataCache` | 关注板块结构化 view model | 切换日期、数据变化 |
| `_todayLeadersCache` | 今日推荐结果 | 日期或推荐条件开关变化 |
| `_dailySectorMapCache` | 日期+类型的板块 Map | 日期/数据变化 |

**新增筛选开关时必须同步检查**：

1. `config.js` 开关变量。
2. `leaders.js`/`calc.js` 条件函数和 `passesLeaderConditions()`。
3. `calcTodayLeaders()` 的缓存 key。
4. `app.js` 的 change 事件和刷新逻辑。
5. `index.html` 的 checkbox。
6. `test/leaders.test.js` 与 `test/page-smoke.test.js`。

---

## 5. 首页今日推荐：当前实际生效条件

实际总入口：`leaders.js:passesLeaderConditions()`。定义存在但被注释的条件不生效；不要只看条件函数名。

### 5.1 当前生效

| 条件 | 当前规则 | 配置/开关 | 实际状态 |
|---|---|---|---|
| A | 股票连续主力净流入天数 ≥ 1 | `leaderCondMinDays()` | **暂时关闭**，调用行已注释 |
| B | 至少一个所属板块进入关注板块 | `LEADER_COND_FOCUS_REQUIRED` | 生效，默认 `true`，页面“关联关注板块”可切换 |
| D | `昨日×0.9 < 当日成交额 < 昨日×1.6` | `RATIO_TURNOVER_LOW=0.9`、`RATIO_TURNOVER_HIGH=1.6` | 生效，缺昨日数据不通过 |
| I | 当日成交量 > 昨日成交量时，涨幅 < 5%（放量大跌不限制）；未放量不限制 | `CHANGE_LIMIT_PCT=5` | 生效，无比较数据不通过。2026-08-07 由「abs(涨跌幅)<5%」改为「涨幅<5%」 |
| J | 当日最高价 > 前一日最高价 | `LEADER_COND_HIGH_HIGHER` | 生效，默认 `true`，页面“最高价突破”可切换 |
| K | 收盘价/开盘价 < 1.03 | `CLOSE_OPEN_RATIO_MAX=1.03`、`LEADER_COND_CLOSE_OPEN_RATIO` | 生效，默认 `true`，页面“收盘/开盘比”可切换 |
| L | 5日均价 ≥ 10日均价 | `LEADER_COND_AVG5_GE_AVG10` | 生效，默认 `true`，页面“5日>=10日均价”可切换 |
| M | 阳线：收盘价 > 5日均价；阴线：最低价 ≤ 5日均价 | `LEADER_COND_CLOSE_ABOVE_AVG5` | 生效，默认 `true`，页面“5日线限制”可切换 |
| N | 排除所属板块在行业/概念主力净流入前 3 名的股票 | `LEADER_COND_EXCLUDE_HOT` | 生效，默认 `true`，页面“排除热门”可切换 |

### 5.2 当前关闭

| 条件 | 规则 | 关闭位置 |
|---|---|---|
| C | 当日成交额 > 昨日×0.9 | `leaders.js` 调用行注释；D 已包含该下限 |
| E | 最大连续流入板块当日成交额 < 昨日×1.6 | `leaders.js` 调用行注释 |
| F | 高天数所属板块当日成交额 > 昨日×0.9 | `leaders.js` 调用行注释 |
| G | 股票连续流入天数在板块最大天数 ±1 范围内 | `leaders.js` 调用行注释；页面“天数±1”仅供全部股票弹窗使用 |
| H | 当日成交量 < 近 5 日（不含当日）最大成交量 | `leaders.js` 调用行注释 |

**注意**：A 关闭后，推荐结果里的 `stockDays` 仍会计算、展示和排序；只是不会再作为入选门槛。

### 5.3 推荐缓存

`_todayLeadersCache` 当前包含：

```text
dateFile
highHigher
focusRequired
closeOpenRatio
avg5GeAvg10
closeAboveAvg5
excludeHot
value
```

任何推荐开关变化必须让缓存失效，否则会出现 UI 已切换但推荐列表仍使用旧结果。

---

### 5.4 回测结论（2026-08-06 更新）

数据源：`test/backtest.js` / `test/backtest_deep.js`，36 个交易日（2026-06-18 ~ 2026-08-06），5596 个配对样本（T 日信号 → T+1 涨跌幅）。结果写入 `outputs/backtest-results.json`（60 条件）和 `outputs/backtest-deep-results.json`（42 条件，含日均一致性）。

**核心结论：主力资金流向与次日涨跌呈负相关。**

| 信号条件 | 样本数 | 次日胜率 | 均涨 | 盈亏比 | 备注 |
|---|---|---|---|---|---|
| 基线（无条件） | 5596 | 47.0% | -0.56% | - | 均值回归效应 |
| 涨+主力净流出>1亿 | 612 | 69.4% | +1.23% | 0.78 | 核心信号，胜率高但均亏>均盈 |
| 涨>3%+净流出>1亿+流出占比>3% | 188 | 71.3% | +3.37% | 1.40 | 最佳组合之一 |
| 涨停(>9.5%)+净流出>1亿 | 41 | 87.8% | +7.97% | 1.77 | 样本最少，胜率最高 |
| 涨停(>9.5%)+净流出>0 | 78 | 68.0% | +4.96% | 1.68 | 均涨显著为正 |
| 涨+净流出>5亿 | 175 | 79.4% | +2.11% | 0.80 | 大额流出反向 |
| 跌+主力净流入>1亿 | 352 | 24.7% | -2.01% | - | 反面信号 |
| 跌>3%+净流入>1亿 | 173 | 21.4% | -3.41% | - | 最弱组合 |

**解读**：
- 涨+主力净流出、涨停+净流出是"主力借涨出货"的反向信号，次日大概率继续涨（散户接盘推动），但需注意均亏幅度大，单次回撤可达 -20%，实盘需止损。
- 跌+主力净流入是"主力抄底但趋势未反转"信号，次日继续跌概率高，应回避。
- 局限：仅 36 个交易日，未计交易成本；样本数越少（如涨停+净流出>2亿 仅 24 例）统计意义越弱。

---

## 6. 关注板块：当前实际条件

实际入口：`calc.js:filterSectors()`，首页和趋势弹窗复用 `getFocusSectors()` / `calcFocusSectorsData()`。

板块必须同时满足：

1. **主力净额 > 0**。
2. **排除占位名称**：`所属行业`、`所属概念`。
3. **成交额不过度放量**：当日成交额 < 昨日成交额×1.6；缺少昨日对应板块数据 → 不通过。
4. **成交额趋势**：
   - 缺少当前或昨日 → 不通过。
   - 昨日存在、前日或前前日缺失 → 降级为当日成交额 > 昨日×0.9。
   - 四日齐全：连续缩量后要求当日 > 昨日×0.9；昨日放量要求当日继续放量；缩量后超过前日视为反弹通过；其他走势不通过。
5. **连续主力净流入天数 ≥ `FOCUS_MIN_DAYS=1`**。

首页还有一个独立开关：

- `FOCUS_EXCLUDE_HOT` 默认配置值为 `false`。
- `index.html` 中“关注板块-排除热门” checkbox 当前写为 `checked`。
- 这两者存在初始显示不一致风险：页面首次加载时计算使用 JS 配置值，用户手动切换后才会按 checkbox 状态同步。修改该区域时优先统一默认值和 UI 状态。

---

## 7. 图表、弹窗和交互

### 主图

`charts.js`：

- 行业/概念各一张横向柱状图。
- 流入/流出单选分别控制图表。
- 红色表示主力净流入，绿色表示主力净流出，符合中国市场习惯。
- 当前数据叠加前一交易日虚线。
- 柱条、柱条所在整行空白区域、Y 轴板块文字均可点击。
- 点击通过 `openFocusSector()` 打开板块趋势详情；前一日虚线不作为跳转目标。

### 趋势弹窗

`modals.js`：

- 左侧：板块近 `TREND_CHART_DAYS=10` 日主力净额/成交额趋势，或个股图表。
- 个股图表默认新浪，支持分时/日K/周K/月K/全部；TradingView 按需异步加载。
- 右侧页签当前包括：我的预选、全部股票、涉及股票、关注板块。
- 表格行点击可打开个股行情；预选状态使用 `localStorage` 持久化，key 为 `preselectedStocks`（v2 格式 `{version:2, items:[stockKey...]}`）。
- 关联板块标签点击可切换趋势视图。
- 首页关注板块和主图点击都进入同一板块详情链路。

### 键盘快捷键

`app.js:initKeyboardShortcuts()`：

- `Esc`：关闭当前弹窗（趋势弹窗优先于全部板块弹窗）。
- `←` / `→`：切换日期（按交易日排序，向最新/最早方向移动）。
- `1` ~ `9`：快速选择日期按钮列表中第 N 个日期。

### 悬浮便签

`app.js`：

- `localStorage` key：`floatingNoteV1`。
- 内容、位置、折叠状态持久化；服务端同步到根目录 `note.txt`。
- 正文样式固定红色加粗，14px。
- 文本加载时识别所有 `M月D日` 格式并替换为当天日期，例如 `7月30日` 会在刷新时变成当天日期。
- 不使用 `{{date}}` 占位符。
- 保存时写入 textarea 原始文本。

---

## 8. 服务端接口与安全边界

`server.js` 为原生 HTTP 服务：

| 方法 | 路径 | 作用 |
|---|---|---|
| GET/HEAD | `/api/list` | 返回 manifest，支持 ETag/304 |
| POST | `/api/parse` | multipart Excel 上传、解析、转 v3、原子写入 JSON |
| GET | `/api/note` | 读取根目录 `note.txt` |
| POST | `/api/note` | JSON `{text:string}`，串行写入 `note.txt` |
| GET/HEAD | 静态路径 | 返回根目录内静态文件 |

已实现的防护：

- 服务默认仅监听 `127.0.0.1`。
- 上传请求体默认上限 20 MiB；便签 POST 上限 1 MiB。
- 先检查 Content-Length，chunked 请求也检查实际字节数。
- multipart boundary 校验、上传文件名清洗、扩展名白名单。
- JSON/Excel 解析失败返回结构化错误，不写入无效结果。
- Excel 成功写入使用 `writeJsonAtomic()`，临时文件成功后 rename。
- 静态路径使用 `path.relative()` 防路径穿越。
- 静态资源支持 Brotli/gzip、ETag、304、Cache-Control。
- 页面 HTML 拼接普遍使用 `escapeHtml()`。

**安全注意**：`xlsx` 版本存在上游安全修复滞后问题；不要把 `/api/parse` 直接暴露到公网。若要公网部署，应加认证、反向代理和更严格的上传隔离。

---

## 9. 数据处理与迁移文件

| 文件 | 用途 | 关键注意 |
|---|---|---|
| `analyze.js` | Excel 列识别、严格数值解析、板块聚合、诊断 | 同一物理列不可重复映射；无有效行/字段冲突时拒绝生成 |
| `reprocess.js` | 旧 JSON + Excel 历史重处理 | 默认 dry-run；`--apply` 才写入 |
| `convert-schema-v3.js` | v2/v1 → v3 去重转换 | 推荐先 dry-run 再 apply |
| `lib/schema-v3.js` | `toSchemaV3()`、`validateSchemaV3()` | 股票字典按 stockKey 去重；板块仅保留股票键 |
| `lib/manifest.js` | 日期解析、价格识别、同日择优 | 改日期口径时必须同步 manifest 测试 |
| `lib/file-utils.js` | 原子 JSON 写入 | 不要改为直接覆盖写入 |
| `generate-list.js` | 静态部署清单 | 数据文件变化后重新生成 `list.json` |
| `query-stocks.js` | 基于 JSON 配置的跨日期通用股票筛选 | `intersection` 要求全部条件；`union` 任一条件即可。示例配置见根目录 `query.json`（多日期字段比较：`refDate`/`refField` 支持跨日期，如当日 high > 前日 high） |

`data.db` 当前未接入运行链路；当前系统以 `data/*.json` + manifest 为准。

---

## 10. 测试地图

| 测试文件 | 覆盖内容 |
|---|---|
| `analyze.test.js` | Excel 数值、列识别、股票代码、聚合、诊断、OHLC |
| `data-lru.test.js` | LRU、日期覆盖、日期按钮 |
| `focus-conditions.test.js` | 关注板块历史缺失和成交额条件 |
| `leaders.test.js` | 今日推荐各条件开关切换、缓存失效、加星一致性 |
| `page-smoke.test.js` | DOM、脚本顺序、主图点击、TradingView 按需加载、便签、推荐/关注开关 |
| `manifest.test.js` | 日期解析、同日择优、价格字段识别、list 生成 |
| `schema-v3.test.js` | v3 去重、水合、引用校验 |
| `stocks.test.js` | 股票 key、金额/成交量单位、旧新格式兼容 |
| `query-stocks.test.js` | intersection/union、跨日期字段比较 |
| `reprocess.test.js` | dry-run、迁移一致性和异常阻断 |
| `server.test.js` | API、上传限制、multipart、静态缓存、便签接口 |
| `inspect_data.js` / `check_fields.js` / `check_prices.js` / `backtest*.js` | 数据检查、字段检查、回测诊断脚本；会输出诊断结果，部分脚本会写 `outputs/`（回测结果在 `outputs/backtest-results.json`、`outputs/backtest-deep-results.json`） |

推荐验证顺序：

```bash
npm test
npm run check
git diff --check
```

只改前端筛选条件时，至少运行：

```bash
npm test
npm run check
```

---

## 11. 修改注意事项（踩坑记录）

1. **条件开关三处联动**：开关变量、`passesLeaderConditions()` 调用行、`_todayLeadersCache` 缓存 key 必须同步，否则出现 UI 与结果不一致。
2. **“暂时关闭”的语义**：注释调用行即可恢复，删除条件函数会更难恢复。项目内已有多处“暂时关闭”注释。
3. **历史缺失规则偏严**：关注板块要求可验证的跨日数据；缺昨日即不通过，缺更早日期降级比较。新增历史对比条件时保持同一口径。
4. **成交额单位为亿（数值）**：`_stockFieldIndex` 中 `amount` 是亿数值；板块级 `成交额`/`主力净额` 是原始元。换算展示时注意除以 1e8。
5. **红色=涨/流入，绿色=跌/流出**：全项目遵循中国市场习惯，不要改成欧美配色。
6. **不要破坏脚本加载顺序**：`page-smoke.test.js` 会校验脚本顺序和关键 DOM。
7. **静态部署兼容**：`/api/list` 不可用时回退 `list.json`，改动数据加载链路时保持该回退。
8. **HTML/JS/CSS 缓存策略为 no-cache + ETag**：静态资源变更后无需手动清浏览器缓存，但需要服务端重启后目录指纹/ETag 生效。
9. **新增数据字段**：需要在 `stock-utils.js` 解析、`storeDataForDate()` 索引、`lib/schema-v3.js` compactStock、`query-stocks.js` VALID_FIELDS 多处同步。
10. **注释使用中文字符**：源码注释大量使用中文，保持一致性；不要在 JSON/字符串里引入全角引号破坏语法。
11. **悬浮便签日期替换**：只替换 `M月D日` 格式文本；不要改成占位符方案（用户明确要求移除）。
12. **代码检查工具**：修改后先 `node --check` 或 `npm run check` 再交付，避免括号/引号失配。

---

## 12. 近期功能变更记录

- **2026-08-05**：今日推荐条件 A（连续净流入天数≥1）暂时关闭。提交 `63ada6a`。
- **2026-08-04**：条件 D 明确为 `昨日×0.9 < 当日成交额 < 昨日×1.6`（代码已是双判断，仅修正注释）；条件 G 暂时关闭；条件 I 涨跌幅限制由 20% 改回 5%。提交 `455decc`。
- **2026-07-30**：今日推荐新增“关联关注板块”“收盘/开盘比”开关；新增条件 L（5日均价≥10日均价）、M（5日线限制）、N（排除热门）；关注板块新增“排除热门”开关；便签支持自动识别 `M月D日` 日期并替换为当天。提交 `2f5fcda` 等。
- **更早**：主图整行点击、板块详情跳转、关注板块历史缺失规则收紧、悬浮便签默认红色加粗、系统改名 GP分析系统、TradingView 按需加载。

---

## 13. 后续修改快速定位表

| 想改什么 | 先看哪里 |
|---|---|
| 今日推荐加/关条件 | `leaders.js:passesLeaderConditions()` → `config.js` 阈值/开关 → `calc.js` 条件函数 |
| 关注板块规则 | `calc.js:filterSectors()`、`calc.js:isSectorTurnoverNotTooLow()` |
| 推荐结果不刷新 | 检查 `_todayLeadersCache` 缓存 key 是否包含新开关 |
| 图表颜色/样式 | `charts.js`、`style.css` |
| 主图点击行为 | `charts.js:createChart()` 的 onClick/onHover |
| Excel 解析列 | `analyze.js:detectColumns()` |
| 数据日期排序 | `lib/manifest.js`、`data.js:sortDateFileList()` |
| 上传限制 | `server.js` `DEFAULT_MAX_REQUEST_BYTES`、`config.js` `MAX_UPLOAD_BYTES` |
| 便签逻辑 | `app.js:initFloatingNote()` |
| 弹窗表格 | `modals.js:renderStockTable()`、`renderAllStocksPanel()` |
